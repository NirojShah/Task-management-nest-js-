DB_HOST=localhost        # Replace with  DB host (e.g., 'localhost' or an IP address)
DB_PORT=3306             # Default MySQL port, change if needed
DB_USER=root             #  MySQL username
DB_PASS=yourpassword     #  MySQL password
DB_NAME=yourdatabase     # The name of  MySQL database
